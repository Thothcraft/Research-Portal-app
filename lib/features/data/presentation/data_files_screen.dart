import 'package:flutter/material.dart';

class DataFilesScreen extends StatelessWidget {
  const DataFilesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Data Files')),
      body: const Center(child: Text('Data Files Screen - Under Development')),
    );
  }
}
